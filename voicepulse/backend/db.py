from motor.motor_asyncio import AsyncIOMotorClient
from config import cfg

client = AsyncIOMotorClient(cfg.MONGO_URI)
pulse_db = client[cfg.DB_NAME]
calls = pulse_db.calls
customers = pulse_db.customers
conversations = pulse_db.conversations
critiques = pulse_db.critiques
from motor.motor_asyncio import AsyncIOMotorClient
from config import cfg

client = AsyncIOMotorClient(cfg.MONGO_URI)
pulse_db = client[cfg.DB_NAME]
calls = pulse_db.calls
customers = pulse_db.customers
conversations = pulse_db.conversations
critiques = pulse_db.critiques
from motor.motor_asyncio import AsyncIOMotorClient
from config import cfg

client = AsyncIOMotorClient(cfg.MONGO_URI)
pulse_db = client[cfg.DB_NAME]
calls = pulse_db.calls
customers = pulse_db.customers
conversations = pulse_db.conversations
critiques = pulse_db.critiques
from motor.motor_asyncio import AsyncIOMotorClient
from config import cfg

client = AsyncIOMotorClient(cfg.MONGO_URI)
pulse_db = client[cfg.DB_NAME]
calls = pulse_db.calls
customers = pulse_db.customers
